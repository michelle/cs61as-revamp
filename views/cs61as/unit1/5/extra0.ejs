<!-- Extra for Experts 5: Hierarchical Data -->

<p>
Read section 2.3.4 and do exercises 2.67-2.72.

<p>
<i>Programming by example:</i>  In some programming systems, instead of
writing an algorithm, you give examples of how you'd like the program to
behave, and the language figures out the algorithm itself:

<p>
<tt>  <pre>&#62; (define pairup (regroup '((1 2) (3 4) ...)))
&#62; (pairup '(the rain in spain stays mainly on the plain))
((the rain) (in spain) (stays mainly) (on the))
</pre></tt>

<p>
Write <tt>regroup</tt>. Read <tt>&#126;cs61as/lib/regroup.problem</tt> for details.

<p>

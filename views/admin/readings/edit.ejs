<div id="centered">
  <div class="yellownote">
    <div class="padding">
      <strong>Warning: </strong>Think twice before you click save.
      This is dangerous, and it has the potential to break the DB (no security yet).
    </div>
  </div>
  <div id="lesson" class="noselect">
    <h2>Editing reading with ID: <strong><%-reading.id%></strong></h2>
  </div>
  <div id="lefttriangle"></div>
  <br>
  <div class="contentpadding">
    <form method="post" action="/admin/readings/edit/<%-reading.id%>">
      <div class="formbox">
        <h3>Add a new Reading</h3>
        <span class="left boxy noselect" required>name</span>
        <input type="text" class="right textboxy" name='reading[name]' value="<%-reading.name%>" required >
        <span class="left boxy noselect" required>location</span>
        <input type="text" class="right textboxy" name='reading[location]' value="<%-reading.location%>" required >
        <span class="left boxy noselect" required>Is SICP</span>
        <% if (reading.SICP) { %>
          <input type="checkbox" class="right textboxy" checked="checked" value="true" name='reading[SICP]' >
        <% } else { %>
          <input type="checkbox" class="right textboxy" value="true" name='reading[SICP]' >
        <% } %>
        <div class="clear"></div>
      </div>
      <button class="anotherbutton" id="submitReading">
        Save
      </button>
    </form>
  </div>
</div>